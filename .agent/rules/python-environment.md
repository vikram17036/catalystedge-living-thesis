---
trigger: always_on
---

Always activate the conda environment named 'stocksense' before starting the backend, and do not install packages globally; only install packages in the 'stocksense' environment.

